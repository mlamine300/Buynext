{
  "name": "@repo/db",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --skip-generate --schema=./prisma/schema.prisma",
    "db:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "dotenv": "^16.0.3",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "prisma": "^6.18.0",
    "prisma-generator-zod": "^1.0.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
